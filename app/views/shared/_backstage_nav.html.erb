<nav class='fixed left-0 flex flex-col justify-between h-full bg-darkBlue'
      data-controller='nav'
>

  <div class="flex flex-col text-center">
    <img src="/assets/logo.svg" alt="logo" class='w-16 pb-6 mx-auto mt-6'>

    <%= link_to '訂位', backstage_restaurant_reservations_path(@restaurant), class: 'backstage-nav-btn'%>

    <%= link_to '資訊', backstage_restaurant_path(@restaurant), class:'backstage-nav-btn' %>

    <%= link_to '座位', backstage_restaurant_seats_path(@restaurant), class:'backstage-nav-btn' %>

    <%= link_to backstage_restaurant_opening_times_path(@restaurant), class:'backstage-nav-btn' do %>
      <i class="text-lg fa-solid fa-gear"></i>
    <% end %>
    
  </div>

  <div class='text-center'>
    <%= link_to "返回", backstage_root_path, class: "backstage-nav-btn text-gray-300 text-sm block" %>
    <h1 class='text-sm text-center text-gray-300'>
      <%= @restaurant.title %>
    </h1>
    <p class='text-sm text-center text-gray-300'>
      <%= @restaurant.branch %>
    </p>
  </div>
</nav>